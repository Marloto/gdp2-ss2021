package otherTest;

public class MainSamePackage {
    public static void main(String[] args) {
        Base b1 = new Base();
        // Frage: Was sollte auf jedenfall sichtbar sein?
        // -> public
        // Frage: was ist sichtbar?
        // -> a, b, c, f(), g(), h()
        
    }
}
